````bash
  # 安装typescript
  npm install typescript -g
  yarn global add typescript
  # 使用 tsc 命令转成 js 文件, 再运js
  tsc demo.ts
  node demo.js

  #直接运行ts文件可以使用ts-node
  #安装ts-node
  npm install ts-node -g

  #运行ts
  ts-node demo.ts

````